port: ":8081"
backendURL: "http://localhost:9090"

db:
  host: "localhost"
  port: 33060
  user: "root"
  password: "root"
  database: "gateway"
  maxIdleConns: 10
  maxOpenConns: 100
  debug: true

redis:
  addr: ""              # Redis地址，如: localhost:6379（为空则不启用Redis）
  password: ""          # Redis密码
  db: 0                 # Redis数据库编号(0-15)
  poolSize: 10          # 连接池大小

log:
  path: ""
  level: "debug"

vmPool:
  size: 100
